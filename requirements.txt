# DB drivers
mysql-connector-python>=8.3.0
oracledb>=2.0.0

# Web framework + server
fastapi>=0.110.0
uvicorn>=0.29.0

# Native window wrapper
pywebview>=4.4.0

# Secure password storage (OS keychain)
keyring>=25.0.0

# Packaging (optional — uncomment to build a standalone .app / .exe)
# pyinstaller>=6.0
